<div class="item {% if loop.index0 == 0 %}active{% endif %}">
    <img src="{{ slide.episode.posterframe|path}}" alt="{{ slide.episode.name }}">
    <div class="carousel-caption">
        {{ slide.episode.name }}
    </div>
</div>

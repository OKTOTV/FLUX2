<div class="item {% if loop.index0 == 0 %}active{% endif %}">
    <a href="{{ path('oktothek_show_episode', {'uniqID': slide.uniqID}) }}">
        <img src="{{ slide.posterframe|path}}" alt="{{ slide.name }}" style="width:100%">
        <div class="carousel-caption">
            {{ slide.name }}
        </div>
    </a>
</div>

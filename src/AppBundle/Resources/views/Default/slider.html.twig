<div id="carousel" class="carousel slide">
  <!-- Indicators -->
  <ol class="carousel-indicators">
      {% for slide in slides %}
        <li data-target="#carousel" data-slide-to="{{ loop.index0 }}" {% if loop.index0 == 0 %}class="active"{% endif %}></li>
      {% endfor %}
  </ol>

  <!-- Wrapper for slides -->
  <div class="carousel-inner" role="listbox">
    {% for slide in slides %}
        <article class="item {% if loop.index0 == 0 %}active{% endif %}" itemscope itemtype="http://schema.org/NewsArticle">
        <figure>
            <a href="{{ slide.link }}"><img src="{{ slide.asset|thumb(720,1280)}}" alt="{{ slide.name }}" style="width:100%"></a>
                <figcaption class="carousel-caption">
                    <div class="container-bg-opacity">
                        <div class="bg-opacity"></div>
                    </div>
                    <a href="{{ slide.link }}">
                    <div class="content-opacity">
                        <meta itemprop="author" content="Okto Marketing" />
                        <meta itemprop="publisher" content="Okto Marketing" />
                        <meta itemprop="datePublished" content="" />
                        <meta itemprop="image" content="{{ slide.asset|thumb(720,1280)}}" />
                        <meta itemprop="mainEntityOfPage" content="{{ slide.link }}" />
                        <p class="type" itemprop="about">{{ 'oktothek.slide_type_choice'|transchoice(slide.slidetype) }}</p>
                        <h3 itemprop="headline">{{ slide.name }}</h3>
                        <p class="slide-content subheadline hidden-xs" itemprop="description">{{ slide.description }}</p>
                    </div>
                    </a>
                </figcaption>
            </figure>
        </article>
    {% endfor %}
  </div>
  <!-- Controls -->
  <a class="left carousel-control" href="#carousel" role="button" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="right carousel-control" href="#carousel" role="button" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
    </a>
</div>

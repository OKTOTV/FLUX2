<article class="col-md-3 col-lg-3 col-sm-8 col-xs-12 pin" data-toggle="tooltip" title="{{ series.description }}">
    <a href="{{ path('oktothek_show_series', {'uniqID': series.uniqID}) }}" title="... ansehen">
        <figure class="series-pin pin">
            <img src="{{ series.posterframe|thumb(135,240) }}" class="img-responsive"/>
			<figcaption>
                <h3>{{ series.name }}</h3>
		    </figcaption>
        </figure>
    </a>
</article>

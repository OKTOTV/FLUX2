{% extends "BprsStyleBundle::layout.html.twig" %}

{% block title%}{{ name|default('OKTO')~' | '~tag.text }}{% endblock %}
{% block nav_head%}
    {% include 'BprsStyleBundle::menu.html.twig' with {'selected': 'oktothek', 'dropdown': ''} %}
{% endblock %}

{% set body_class = "tagpage head-white-color head-transparent-bg fullscreen-background" %}
{% block body %}
	<div id="top"></div>
    <div class="container-fluid">
	    <div class="bg-bleur"></div>
		<header>
		    <h1>{{ 'oktothek_tag_header'|trans({'%title%': tag.text})}}</h1>
			{% include "AppBundle::Tag/shareTag.html.twig" with {'tag': tag} %}
	    </header>
		<div class="container content-bg">
		    <section class="inc-pins">
                <h2>{{ 'oktothek_tag_episode_header'|trans }}</h2>
				<div class="pins-wrapper">
                  {{ render(controller('AppBundle:Tag:episodeTag', {'tag': tag.slug})) }}
				</div>
                <a href="{{ path('oktothek_tag_episode_page', {'slug': tag.slug}) }}" class="pull-right more">{{ 'oktothek_tag_more_results'|trans }} <span class="icon">+</span></a>
            </section>
		    <section class="inc-pins">
		        <div id="anchor_series"></div>
		        <h2>Serien</h2>
				<div class="pins-wrapper">
                  {{ render(controller('AppBundle:Tag:seriesTag', {'tag': tag.slug})) }}
				</div>
                <a href="{{ path('oktothek_tag_series_page', {'slug': tag.slug}) }}" class="pull-right more">{{ 'oktothek_tag_more_results'|trans }} <span class="icon">+</span></a>
		    </section>
		    <section class="inc-pins">
		        <div id="anchor_playlists"></div>
		        <h2>Playlisten</h2>
				<div class="pins-wrapper">
                  {{ render(controller('AppBundle:Tag:playlistTag', {'tag': tag.slug})) }}
                </div>
				<a href="{{ path('oktothek_tag_playlists_page', {'slug': tag.slug}) }}" class="pull-right more">{{ 'oktothek_tag_more_results'|trans }} <span class="icon">+</span></a>
		    </section>
		<section>
		    <div id="anchor_posts"></div>
		    <h2>Posts</h2>
            {{ render(controller('AppBundle:Tag:postsTag', {'tag': tag.slug})) }}
            <a href="{{ path('oktothek_tag_posts_page', {'slug': tag.slug}) }}" class="pull-right more">{{ 'oktothek_tag_more_results'|trans }} <span class="icon">+</span></a>
		</section>
		<section>
		    <div id="anchor_pages"></div>
		    <h2>Seiten</h2>
            {{ render(controller('AppBundle:Tag:pagesTag', {'tag': tag.slug})) }}
            <a href="{{ path('oktothek_tag_pages_page', {'slug': tag.slug}) }}" class="pull-right more">{{ 'oktothek_tag_more_results'|trans }} <span class="icon">+</span></a>
        </section>
		</div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style type="text/css">
        .bg-bleur {
            {% if background_episode is not empty %}
                background-image:url( {{ background_episode.posterframe|path() }});
            {% endif %}
        }
    </style>
{% endblock %}

<ul class="nav">
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-globe"></span> SPRACHEN</a>
        <ul class="dropdown-menu" role="menu">
            <li><a href="{{ path('homepage', {'_locale': 'de'}) }}">Deutsch</a></li>
            <li><a href="{{ path('homepage', {'_locale': 'en'}) }}">English</a></li>
            <li></li>
        </ul>
    </li>
    <li {% if slug|default('') == 'oktothek' %}class="active"{% endif %}><a href="{{ path('homepage') }}">Home</a></li>
    <li {% if slug|default('') == 'oktothek_news' %}class="active"{% endif %}><a href="{{ path('oktothek_news')}}">News</a></li>
    {% for page in pages %}
        <li {% if slug|default('') == page.slug %}class="active"{% endif %}><a href="{{ path('oktothek_page_show', {'page': page.slug}) }}">{{ page.title }}</a></li>
    {% endfor %}
    <a href="{{ path('oktothek_page_new') }}" class="btn btn-default">{{ 'oktothek.page_create_new_button'|trans|raw }}</a>
</ul>

{% extends "BprsStyleBundle::layout.html.twig" %}

{% block nav_head%}
    {% include 'BprsStyleBundle::menu.html.twig' with {'selected': 'oktothek', 'dropdown': ''} %}
{% endblock %}

{% set body_class = "series fullscreen-images" %}
{% block body_main %}

<div class="page-header">
    <h1>{{ series.name}}</h1>
    <p>
        <a href="{{ path('oktothek_show_series', {'uniqID': series.uniqID }) }}" class="btn btn-default">Zurück zur Sendereihe</a>
    </p>
</div>

<section class="blog">
<div class="row">
    <div class="navigation">
        {{ knp_pagination_render(posts) }}
    </div>
    {% include 'AppBundle::Default/Series/blog.html.twig' with {'posts': posts, 'newsloop': 1} %}
    <div class="navigation">
        {{ knp_pagination_render(posts) }}
    </div>
</div>
</section>

{% endblock %}

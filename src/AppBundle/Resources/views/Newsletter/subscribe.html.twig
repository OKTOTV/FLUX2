{{ form_start(form) }}
    <div class="input-group">
        {{ form_widget(form.email) }}
        {{ form_row(form.submit, {'attr': {'class': 'input-group-addon'}}) }}
    </div>
{{ form_end(form) }}
